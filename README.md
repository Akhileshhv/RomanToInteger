# RomanToInteger
This commit adds a Java program that converts Roman numerals (I, V, X, L, C, D, M) to their corresponding integer values.  Features: - Uses a HashMap to map Roman symbols to integer values.  This is a console-based utility written in a single Java file for simplicity.
